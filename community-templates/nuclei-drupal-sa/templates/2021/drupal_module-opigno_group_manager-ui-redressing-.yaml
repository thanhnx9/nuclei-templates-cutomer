
id: drupal-module-opigno-group-manager-ui-redressing-
info:
  name: drupal-module-opigno-group-manager-ui-redressing-
  author: Bishopfox
  severity: medium
  description: "This project is related to Opigno LMS distribution. It implements the group manager in the Opigno LMS.  The module does not set X-Frame-Options and blocks ability of other modules (e.g Security Kit) to add them, leaving it vulnerable to Clickjacking."
  reference:
    - https://www.drupal.org/sa-contrib-2021-019
  metadata:
    security-risk: "Less critical 9∕25 AC:Complex/A:None/CI:None/II:None/E:Theoretical/TD:All"
    vulnerability: "ui-redressing-"
    fofa-query: "/sites/all/modules/opigno-group-manager/"
    google-query: "inurl:'/sites/all/modules/opigno-group-manager/"
    impact: medium
    type: indicator
    created-at: '0001-01-01T00:00:00Z'
  tags: drupal

http:
  - method: GET
    redirects: true
    max-redirects: 3
    path:
      - "{{BaseURL}}/sites/all/modules/opigno-group-manager/opigno-group-manager.info"

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'
      - type: status
        status:
          - 200
      - type: word
        words:
          - 'opigno-group-manager'
        part: body

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'

      - type: dsl
        dsl:
         - compare-versions(version, '8.x-1.7')
         - compare-versions(version, '8.x-1.6')
         - compare-versions(version, '8.x-1.5')
         - compare-versions(version, '8.x-1.4')
         - compare-versions(version, '8.x-1.3')
         - compare-versions(version, '8.x-1.2')
         - compare-versions(version, '8.x-1.1')
         - compare-versions(version, '8.x-1.0')
