
id: drupal-module-file-chooser-field-multiple-issues
info:
  name: drupal-module-file-chooser-field-multiple-issues
  author: Bishopfox
  severity: medium
  description: "The File Chooser Field allows users to upload files using 3rd party plugins such as Google Drive and Dropbox.  This module fails to validate user input sufficiently which could under certain circumstances lead to a Server Side Request Forgery (SSRF) vulnerability leading to Information Disclosure. In uncommon configurations and scenarios, it might lead to Remote Code Execution."
  reference:
    - https://www.drupal.org/sa-contrib-2023-015
  metadata:
    security-risk: "Moderately critical 14∕25 AC:Basic/A:User/CI:Some/II:None/E:Exploit/TD:All"
    vulnerability: "multiple-issues"
    fofa-query: "/sites/all/modules/file-chooser-field/"
    google-query: "inurl:'/sites/all/modules/file-chooser-field/"
    impact: medium
    type: indicator
    created-at: '0001-01-01T00:00:00Z'
  tags: drupal

http:
  - method: GET
    redirects: true
    max-redirects: 3
    path:
      - "{{BaseURL}}/sites/all/modules/file-chooser-field/file-chooser-field.info"

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'
      - type: status
        status:
          - 200
      - type: word
        words:
          - 'file-chooser-field'
        part: body

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'

      - type: dsl
        dsl:
         - compare-versions(version, '7.x-1.12')
         - compare-versions(version, '7.x-1.11')
         - compare-versions(version, '7.x-1.10')
         - compare-versions(version, '7.x-1.9')
         - compare-versions(version, '7.x-1.8')
         - compare-versions(version, '7.x-1.7')
         - compare-versions(version, '7.x-1.6')
         - compare-versions(version, '7.x-1.5')
         - compare-versions(version, '7.x-1.4')
         - compare-versions(version, '7.x-1.3')
         - compare-versions(version, '7.x-1.2')
         - compare-versions(version, '7.x-1.1')
         - compare-versions(version, '7.x-1.0')
