
id: drupal-module-filefield-sources-access-bypass
info:
  name: drupal-module-filefield-sources-access-bypass
  author: Bishopfox
  severity: medium
  description: "This module enables you to upload files to fields via several sources. The module doesnt sufficiently handle access control under the scenario of the autocomplete path of reference sources."
  reference:
    - https://www.drupal.org/sa-contrib-2018-007
  metadata:
    security-risk: "Moderately critical 12∕25 AC:Complex/A:None/CI:Some/II:None/E:Theoretical/TD:All"
    vulnerability: "access-bypass"
    fofa-query: "/sites/all/modules/filefield-sources/"
    google-query: "inurl:'/sites/all/modules/filefield-sources/"
    impact: medium
    type: indicator
    created-at: '0001-01-01T00:00:00Z'
  tags: drupal

http:
  - method: GET
    redirects: true
    max-redirects: 3
    path:
      - "{{BaseURL}}/sites/all/modules/filefield-sources/filefield-sources.info"

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'
      - type: status
        status:
          - 200
      - type: word
        words:
          - 'filefield-sources'
        part: body

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'

      - type: dsl
        dsl:
         - compare-versions(version, '7.x-1.10')
         - compare-versions(version, '7.x-1.9')
         - compare-versions(version, '7.x-1.8')
         - compare-versions(version, '7.x-1.7')
         - compare-versions(version, '7.x-1.6')
         - compare-versions(version, '7.x-1.5')
         - compare-versions(version, '7.x-1.4')
         - compare-versions(version, '7.x-1.3')
