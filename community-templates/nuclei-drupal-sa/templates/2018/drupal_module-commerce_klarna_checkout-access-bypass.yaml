
id: drupal-module-commerce-klarna-checkout-access-bypass
info:
  name: drupal-module-commerce-klarna-checkout-access-bypass
  author: Bishopfox
  severity: medium
  description: "The Commerce Klarna Checkout module enables you to accept payments from the Klarna Checkout payment provider  The module doesnt sufficiently validate the payment callback made by Klarna. An attacker could bypass the payment step."
  reference:
    - https://www.drupal.org/sa-contrib-2018-062
  metadata:
    security-risk: "Moderately critical 13∕25 AC:Basic/A:None/CI:None/II:Some/E:Theoretical/TD:All"
    vulnerability: "access-bypass"
    fofa-query: "/sites/all/modules/commerce-klarna-checkout/"
    google-query: "inurl:'/sites/all/modules/commerce-klarna-checkout/"
    impact: medium
    type: indicator
    created-at: '0001-01-01T00:00:00Z'
  tags: drupal

http:
  - method: GET
    redirects: true
    max-redirects: 3
    path:
      - "{{BaseURL}}/sites/all/modules/commerce-klarna-checkout/commerce-klarna-checkout.info"

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'
      - type: status
        status:
          - 200
      - type: word
        words:
          - 'commerce-klarna-checkout'
        part: body

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'

      - type: dsl
        dsl:
         - compare-versions(version, '7.x-1.x-dev')
         - compare-versions(version, '7.x-1.5')
         - compare-versions(version, '7.x-1.4')
         - compare-versions(version, '7.x-1.3')
         - compare-versions(version, '7.x-1.2')
         - compare-versions(version, '7.x-1.1')
         - compare-versions(version, '7.x-1.0')
