
id: drupal-module-search-api-information-disclosure
info:
  name: drupal-module-search-api-information-disclosure
  author: Bishopfox
  severity: medium
  description: "This module enables you to build searches using a wide range of features, data sources and backends. The module doesnt in all cases correctly detect whether a given search is active on the current page, leading to potential information disclosure for some setups. This vulnerability is mitigated by the fact that only very specific setups will have this problem and there is no way for an attacker to trigger it."
  reference:
    - https://www.drupal.org/sa-contrib-2022-059
  metadata:
    security-risk: "Moderately critical 13∕25 AC:None/A:None/CI:Some/II:None/E:Theoretical/TD:Uncommon"
    vulnerability: "information-disclosure"
    fofa-query: "/sites/all/modules/search-api/"
    google-query: "inurl:'/sites/all/modules/search-api/"
    impact: medium
    type: indicator
    created-at: '0001-01-01T00:00:00Z'
  tags: drupal

http:
  - method: GET
    redirects: true
    max-redirects: 3
    path:
      - "{{BaseURL}}/sites/all/modules/search-api/search-api.info"

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'
      - type: status
        status:
          - 200
      - type: word
        words:
          - 'search-api'
        part: body

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'

      - type: dsl
        dsl:
         - compare-versions(version, '8.x-1.26')
         - compare-versions(version, '8.x-1.25')
         - compare-versions(version, '8.x-1.24')
         - compare-versions(version, '8.x-1.23')
         - compare-versions(version, '8.x-1.22')
         - compare-versions(version, '8.x-1.21')
         - compare-versions(version, '8.x-1.20')
         - compare-versions(version, '8.x-1.19')
         - compare-versions(version, '8.x-1.18')
         - compare-versions(version, '8.x-1.17')
         - compare-versions(version, '8.x-1.16')
         - compare-versions(version, '8.x-1.15')
         - compare-versions(version, '8.x-1.14')
         - compare-versions(version, '8.x-1.13')
         - compare-versions(version, '8.x-1.12')
         - compare-versions(version, '8.x-1.11')
         - compare-versions(version, '8.x-1.10')
         - compare-versions(version, '8.x-1.9')
         - compare-versions(version, '8.x-1.8')
         - compare-versions(version, '8.x-1.7')
         - compare-versions(version, '8.x-1.6')
         - compare-versions(version, '8.x-1.5')
         - compare-versions(version, '8.x-1.4')
         - compare-versions(version, '8.x-1.3')
         - compare-versions(version, '8.x-1.2')
         - compare-versions(version, '8.x-1.1')
         - compare-versions(version, '8.x-1.0')
