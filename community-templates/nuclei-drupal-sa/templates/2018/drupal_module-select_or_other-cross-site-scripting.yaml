
id: drupal-module-select-or-other-cross-site-scripting
info:
  name: drupal-module-select-or-other-cross-site-scripting
  author: Bishopfox
  severity: medium
  description: "This module enables users to select other on certain form elements and a textfield appears for the user to provide a custom value. The module doesnt sufficiently escape values of a text field the under the scenario when Select or other formatter is used. This vulnerability is mitigated by the fact that an attacker must have access to edit a field that is displayed through the Select or other formatter."
  reference:
    - https://www.drupal.org/sa-contrib-2018-054
  metadata:
    security-risk: "Moderately critical 14∕25 AC:Basic/A:User/CI:Some/II:Some/E:Theoretical/TD:All"
    vulnerability: "cross-site-scripting"
    fofa-query: "/sites/all/modules/select-or-other/"
    google-query: "inurl:'/sites/all/modules/select-or-other/"
    impact: medium
    type: indicator
    created-at: '0001-01-01T00:00:00Z'
  tags: drupal

http:
  - method: GET
    redirects: true
    max-redirects: 3
    path:
      - "{{BaseURL}}/sites/all/modules/select-or-other/select-or-other.info"

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'
      - type: status
        status:
          - 200
      - type: word
        words:
          - 'select-or-other'
        part: body

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'

      - type: dsl
        dsl:
         - compare-versions(version, '7.x-2.23')
         - compare-versions(version, '7.x-2.22')
         - compare-versions(version, '7.x-2.21')
         - compare-versions(version, '7.x-2.20')
         - compare-versions(version, '7.x-2.19')
         - compare-versions(version, '7.x-2.18')
         - compare-versions(version, '7.x-2.17')
         - compare-versions(version, '7.x-2.16')
         - compare-versions(version, '7.x-2.15')
         - compare-versions(version, '7.x-2.14')
         - compare-versions(version, '7.x-2.13')
         - compare-versions(version, '7.x-2.12')
         - compare-versions(version, '7.x-2.11')
         - compare-versions(version, '7.x-2.10')
         - compare-versions(version, '7.x-2.9')
         - compare-versions(version, '7.x-2.8')
         - compare-versions(version, '7.x-2.7')
         - compare-versions(version, '7.x-2.6')
         - compare-versions(version, '7.x-2.5')
         - compare-versions(version, '7.x-2.4')
         - compare-versions(version, '7.x-2.3')
         - compare-versions(version, '7.x-2.2')
         - compare-versions(version, '7.x-2.1')
         - compare-versions(version, '7.x-2.0')
