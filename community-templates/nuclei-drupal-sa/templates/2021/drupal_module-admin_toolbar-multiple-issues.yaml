
id: drupal-module-admin-toolbar-multiple-issues
info:
  name: drupal-module-admin-toolbar-multiple-issues
  author: Bishopfox
  severity: medium
  description: "The Admin Toolbar (admin-toolbar) module extends the default toolbar provided by Drupal Core with various features facilitating day-to-day editorial and administrative work. The Admin Toolbar Search sub-module of this module  doesnt sanitize user input in certain cases, which leads to a Cross-Site-Scripting (XSS) vulnerability. An attacker that can create or edit certain entities, entity bundles or entity types may be able to exploit one or more Cross-Site-Scripting (XSS) vulnerabilities to target users with access to the Admin Toolbar Search search box, including site admins with privileged access. doesnt properly check access in certain cases, which may result in an information disclosure vulnerability of entity type and bundle labels.  The vulnerability is mitigated by the facts, that:  the Admin Toolbar Search sub-module must be enabled. an attacker must have one of several permissions, of which at least some are commonly only assigned to either editors, site builders or administrators. a targeted account must have permission to use the search box provided by the Admin Toolbar Search sub-module."
  reference:
    - https://www.drupal.org/sa-contrib-2021-025
  metadata:
    security-risk: "Moderately critical 13∕25 AC:Basic/A:User/CI:Some/II:Some/E:Theoretical/TD:Default"
    vulnerability: "multiple-issues"
    fofa-query: "/sites/all/modules/admin-toolbar/"
    google-query: "inurl:'/sites/all/modules/admin-toolbar/"
    impact: medium
    type: indicator
    created-at: '0001-01-01T00:00:00Z'
  tags: drupal

http:
  - method: GET
    redirects: true
    max-redirects: 3
    path:
      - "{{BaseURL}}/sites/all/modules/admin-toolbar/admin-toolbar.info"

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'
      - type: status
        status:
          - 200
      - type: word
        words:
          - 'admin-toolbar'
        part: body

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'

      - type: dsl
        dsl:
         - compare-versions(version, '3.0.1')
         - compare-versions(version, '3.0.0')
         - compare-versions(version, '8.x-2.4')
         - compare-versions(version, '8.x-2.3')
         - compare-versions(version, '8.x-2.2')
         - compare-versions(version, '8.x-2.1')
         - compare-versions(version, '8.x-2.0')
         - compare-versions(version, '8.x-1.27')
         - compare-versions(version, '8.x-1.26')
         - compare-versions(version, '8.x-1.25')
         - compare-versions(version, '8.x-1.24')
         - compare-versions(version, '8.x-1.23')
         - compare-versions(version, '8.x-1.22')
         - compare-versions(version, '8.x-1.21')
         - compare-versions(version, '8.x-1.20')
         - compare-versions(version, '8.x-1.19')
         - compare-versions(version, '8.x-1.18')
         - compare-versions(version, '8.x-1.17')
         - compare-versions(version, '8.x-1.16')
         - compare-versions(version, '8.x-1.15')
         - compare-versions(version, '8.x-1.14')
         - compare-versions(version, '8.x-1.13')
         - compare-versions(version, '8.x-1.12')
         - compare-versions(version, '8.x-1.11')
         - compare-versions(version, '8.x-1.10')
         - compare-versions(version, '8.x-1.9')
         - compare-versions(version, '8.x-1.8')
         - compare-versions(version, '8.x-1.7')
         - compare-versions(version, '8.x-1.6')
         - compare-versions(version, '8.x-1.5')
         - compare-versions(version, '8.x-1.4')
         - compare-versions(version, '8.x-1.3')
         - compare-versions(version, '8.x-1.2')
         - compare-versions(version, '8.x-1.1')
         - compare-versions(version, '8.x-1.0')
