
id: drupal-module-simple-amp-access-bypass
info:
  name: drupal-module-simple-amp-access-bypass
  author: Bishopfox
  severity: medium
  description: "This module allows display of a sites content in AMP format. The module doesnt sufficiently check access on unpublished or restricted content."
  reference:
    - https://www.drupal.org/sa-contrib-2019-071
  metadata:
    security-risk: "Moderately critical 13∕25 AC:Basic/A:None/CI:Some/II:None/E:Theoretical/TD:All"
    vulnerability: "access-bypass"
    fofa-query: "/sites/all/modules/simple-amp/"
    google-query: "inurl:'/sites/all/modules/simple-amp/"
    impact: medium
    type: indicator
    created-at: '0001-01-01T00:00:00Z'
  tags: drupal

http:
  - method: GET
    redirects: true
    max-redirects: 3
    path:
      - "{{BaseURL}}/sites/all/modules/simple-amp/simple-amp.info"

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'
      - type: status
        status:
          - 200
      - type: word
        words:
          - 'simple-amp'
        part: body

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - 'version = "([0-9]+\.x-[0-9]+\.[0-9]+)"'

      - type: dsl
        dsl:
         - compare-versions(version, '8.x-1.15')
         - compare-versions(version, '8.x-1.14')
         - compare-versions(version, '8.x-1.13')
         - compare-versions(version, '8.x-1.12')
         - compare-versions(version, '8.x-1.11')
         - compare-versions(version, '8.x-1.10')
         - compare-versions(version, '8.x-1.9')
         - compare-versions(version, '8.x-1.8')
         - compare-versions(version, '8.x-1.7')
         - compare-versions(version, '8.x-1.6')
         - compare-versions(version, '8.x-1.5')
         - compare-versions(version, '8.x-1.4')
         - compare-versions(version, '8.x-1.3')
         - compare-versions(version, '8.x-1.2')
         - compare-versions(version, '8.x-1.1')
